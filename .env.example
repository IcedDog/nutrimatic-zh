# Use an absolute host directory. On Windows, forward slashes are recommended.
NUTRIMATIC_INDEX_DIR=/absolute/path/to/index-directory
NUTRIMATIC_INDEX_FILE=index.ntri
NUTRIMATIC_PORT=8080

# Create a remotely managed tunnel in Cloudflare Zero Trust and paste its token here.
CLOUDFLARE_TUNNEL_TOKEN=
